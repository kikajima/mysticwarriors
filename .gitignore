# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/

# Persistência v0.7 — artefatos locais do beacon (snapshots não são versionados)
db/production-snapshot/.incoming/
db/production-snapshot/custom.db.bak-*
# v0.9.10 — backups físicos do reset geral (ficam no servidor onde o reset roda)
db/backups/

# v0.9.14 — DEPLOY SLIM: artefatos locais que NÃO devem viajar no pacote
# de deployment (o tar de 36MB sozinho derrubava o deploy por tamanho).
/upload/
/backups/
/tool-results/
/download/
/scripts/*.png
*.tar

# Dados reais nunca devem ser versionados
/db/
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
!.env.example

# Pacotes e exportações locais
*.rar
*.zip
*.bak

# Artefatos internos de desenvolvimento / auditoria (podem conter PII ou metadados)
/worklog.md
/scripts/vlm-*.json
